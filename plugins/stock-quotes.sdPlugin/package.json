{
  "name": "@open-streamdeck/plugin-stock-quotes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=plugin.js",
    "dev": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=plugin.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
